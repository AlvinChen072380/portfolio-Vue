# 開發進度

## 2026-06-17 — 深色模式

### 完成項目
- 新增 `@media (prefers-color-scheme: dark)` 至 `App.vue`，完整覆蓋 `:root` 色票
- 新增 10 個語義 CSS token（`--surface-secondary`、`--tag-bg`、`--tag-color`、`--border-color`、`--skill-logo-bg`、`--scroll-hint-bg`、`--placeholder-bg`、`--btn-dark-bg`、`--btn-dark-color`）
- 修正所有元件 hard-coded 顏色改用 token：`SkillCard`、`WorksSection`、`SkillsSection`、`ProjectCardDetail`、`ProfileCard`
- 深色模式同步調整 shadow elevation 與 viewport glow 配色

### 技術決策
- 採 `prefers-color-scheme` media query，無需切換按鈕，跟隨系統設定自動生效
- `#212529`（SkillsSection 效能卡）、玻璃效果 `rgba(255,255,255,0.07/0.08)`、`#ff4757`（愛心）、`#ffd43b`（星星）等設計語義色維持 hard-coded

## 2026-06-17 — 手機版字體修正
- `SkillsSection` 與 `WorksSection` 的 `section-title` 在 `@media (max-width: 768px)` 加上 `font-size: 1.5rem`
